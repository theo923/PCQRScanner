{
  "name": "pc-qr-scanner",
  "version": "1.0.0",
  "description": "Chrome extension to scan QR codes visible on any browser tab",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "icons": "node scripts/generate-icons.js"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "sharp": "^0.33.4"
  }
}
